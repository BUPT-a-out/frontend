int main() {
    int a = 1, b = 2;

    if (a < b || a > 0 && b > 0)
        a = a + 1;

    if (a < b || a > 0 && b > 0)
        a = a + 1;
    else
        a = a - 1;

    while (a < b || a > 0 && b > 0) {
        a = a + 1;
    }

    return 0;
}
